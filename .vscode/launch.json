{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "C/C++: make build and debug active file",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/cub3d", // Cambia esto al nombre de tu ejecutable
            "args": ["maps/map1.cub"], // Argumentos de tu programa
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "/usr/bin/gdb", // Asegúrate de que esta ruta sea correcta
            "preLaunchTask": "build"
        }
    ]
}
